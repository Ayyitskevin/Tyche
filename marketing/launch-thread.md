# X launch thread (5 tweets)

Ready to post from @Nyc_bagels. One idea per tweet, honest posture up front, demo link in the
first and last tweet. Attach the autoplay demo loop (`marketing/demo.gif`) to tweet 1 and a
screenshot to tweet 3.

---

**1/**
I built Tyche: a keyboard-first financial research terminal in your browser.

`AAPL GP` → charting. `ETH DEX` → on-chain pools. `EQS` → screening. `ECO GDP` → macro.

30+ commands, tiling workspaces, streaming quotes. Feels like the $25k/yr terminal, priced like a
SaaS: free to self-host, $29/mo hosted.

Try it, no signup → [demo URL]

**2/**
The honest part, because fintech marketing is full of lies:

Tyche sells software — never market data. Live sources connect under YOUR licenses: some keyless
and public (crypto, FX, on-chain DEX), others with your own free keys (SEC EDGAR, FRED). No advice.
No order routing. A deterministic mock mode runs with zero keys so you can try everything right now.

**3/**
Speed is the product. ⌘K, type, Enter — every panel opens from the keyboard. Fuzzy autocomplete
across 30+ commands. Tab cycles panels. Rebindable chords. Named layouts per task (trading desk /
research / macro). Provenance stamped on every single datum, down to the CSV exports.

**4/**
It's open-core: the entire terminal is Apache-2.0 on GitHub. `docker compose up` and it's yours,
forever, free. The hosted version is for people who want accounts, sync, TLS, backups, and
upgrades handled — 14-day trial, no card, cancel anytime and export everything as JSON.

**5/**
Built solo, in the open, with a public changelog and a real roadmap. If you research markets with
more than a brokerage app — this is for you.

Live demo, no signup: [demo URL]
Hosted w/ accounts: [hosted URL]
Self-host free: github.com/Ayyitskevin/Tyche

Tell me what's missing and I'll probably ship it this week.

---

*Posting notes: keep each tweet under 280 chars if X Premium isn't available — trim tweet 3's
chord list first, then tweet 2's provider parenthetical. Reply to every response for 48h; pin the
thread.*
